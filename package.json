{
  "name": "main",
  "scripts": {
    "start": "cd ./docker && HOST_DOMAIN=worksoft.systems /bin/docker-compose up -d",
    "gen": "cd server && npm run gen",
    "attach": "tmux attach -t dev",
    "stop": "cd ./docker && /bin/docker-compose down"
  }
}
